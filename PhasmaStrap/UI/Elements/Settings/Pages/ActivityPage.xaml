<ui:UiPage x:Class="PhasmaStrap.UI.Elements.Settings.Pages.ActivityPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:resources="clr-namespace:PhasmaStrap.Resources"
      xmlns:controls="clr-namespace:PhasmaStrap.UI.Elements.Controls"
      xmlns:dmodels="clr-namespace:PhasmaStrap.UI.ViewModels.Settings"
      mc:Ignorable="d"
      d:DataContext="{d:DesignInstance dmodels:ActivityViewModel, IsDesignTimeCreatable=False}"
      Title="ActivityPage"
      Scrollable="True">
    <StackPanel Margin="0,0,14,14">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="{x:Static resources:Strings.Menu_Integrations_Tabs_Activity}" FontSize="20" FontWeight="Medium" />
                <TextBlock TextWrapping="Wrap" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{x:Static resources:Strings.Menu_Activity_SessionsHowMuchYouPlayWho}" />
            </StackPanel>
            <ui:Button Grid.Column="1" VerticalAlignment="Top" Content="{x:Static resources:Strings.Dialog_ChannelLists_Refresh}" Icon="ArrowClockwise24" Appearance="Secondary" Command="{Binding RefreshCommand, Mode=OneTime}" />
        </Grid>

        <TabControl Margin="0,12,0,0">
            <!-- ============================================================ timeline -->
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <ui:SymbolIcon Symbol="Timeline24" Margin="0,0,6,0" />
                        <TextBlock Text="{x:Static resources:Strings.Menu_Activity_Timeline}" />
                    </StackPanel>
                </TabItem.Header>
                <StackPanel Margin="0,12,0,0">
                    <StackPanel HorizontalAlignment="Center" Margin="0,28,0,28" Visibility="{Binding TimelineEmptyVisibility, Mode=OneWay}">
                        <ui:SymbolIcon Symbol="Timeline24" FontSize="44" Opacity="0.25" HorizontalAlignment="Center" />
                        <TextBlock Text="{x:Static resources:Strings.Menu_Activity_NoSessionsYetTheyAppearHere}" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="14" Opacity="0.6" Margin="0,10,0,0" HorizontalAlignment="Center" />
                    </StackPanel>

                    <ItemsControl ItemsSource="{Binding Sessions, Mode=OneWay}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="0,0,0,18">
                                    <StackPanel Orientation="Horizontal" Margin="2,0,0,6">
                                        <TextBlock FontSize="14" FontWeight="SemiBold" Text="{Binding Header}" />
                                        <TextBlock FontSize="12" Margin="10,2,0,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{Binding Summary}" />
                                    </StackPanel>

                                    <ItemsControl ItemsSource="{Binding Visits}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Margin="0,0,0,6" Padding="12,10" CornerRadius="8" BorderThickness="1" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" Background="{DynamicResource ControlFillColorDefaultBrush}">
                                                    <StackPanel>
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="Auto" />
                                                                <ColumnDefinition Width="*" />
                                                                <ColumnDefinition Width="Auto" />
                                                            </Grid.ColumnDefinitions>

                                                            <Border Grid.Column="0" Width="40" Height="40" CornerRadius="6" Background="{DynamicResource ControlFillColorSecondaryBrush}" VerticalAlignment="Center">
                                                                <Border.Clip>
                                                                    <RectangleGeometry Rect="0,0,40,40" RadiusX="6" RadiusY="6" />
                                                                </Border.Clip>
                                                                <Image Source="{Binding IconUrl}" Stretch="UniformToFill" RenderOptions.BitmapScalingMode="HighQuality" />
                                                            </Border>

                                                            <StackPanel Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center">
                                                                <TextBlock FontSize="13" FontWeight="SemiBold" Text="{Binding Title}" TextTrimming="CharacterEllipsis" />
                                                                <TextBlock FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{Binding Subtitle}" TextTrimming="CharacterEllipsis" />
                                                                <TextBlock FontSize="11" Margin="0,2,0,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{Binding FriendsText}" Visibility="{Binding FriendsVisibility}" TextTrimming="CharacterEllipsis" />
                                                            </StackPanel>

                                                            <StackPanel Grid.Column="2" Margin="12,0,0,0" VerticalAlignment="Center" Visibility="{Binding SparkVisibility}">
                                                                <Canvas Width="260" Height="36" ClipToBounds="True">
                                                                    <Polyline Points="{Binding Spark}" Stroke="{DynamicResource SystemAccentColorSecondaryBrush}" StrokeThickness="1.5" StrokeLineJoin="Round" />
                                                                </Canvas>
                                                                <TextBlock FontSize="11" HorizontalAlignment="Right" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{Binding FpsText}" />
                                                            </StackPanel>
                                                        </Grid>

                                                        <ItemsControl Margin="52,8,0,0" ItemsSource="{Binding Captures}" Visibility="{Binding CapturesVisibility}">
                                                            <ItemsControl.ItemsPanel>
                                                                <ItemsPanelTemplate>
                                                                    <WrapPanel />
                                                                </ItemsPanelTemplate>
                                                            </ItemsControl.ItemsPanel>
                                                            <ItemsControl.ItemTemplate>
                                                                <DataTemplate>
                                                                    <Border Margin="0,0,6,6" Height="48" MinWidth="48" CornerRadius="5" Cursor="Hand" ToolTip="{Binding FileName}"
                                                                            Background="{DynamicResource ControlFillColorSecondaryBrush}" ClipToBounds="True">
                                                                        <Border.InputBindings>
                                                                            <MouseBinding MouseAction="LeftClick" Command="{Binding DataContext.OpenCaptureCommand, RelativeSource={RelativeSource AncestorType=ui:UiPage}}" CommandParameter="{Binding}" />
                                                                        </Border.InputBindings>
                                                                        <Grid>
                                                                            <Image Source="{Binding Thumbnail}" Height="48" Stretch="Uniform" Visibility="{Binding ThumbnailVisibility}" RenderOptions.BitmapScalingMode="HighQuality" />
                                                                            <ui:SymbolIcon Symbol="{Binding Symbol}" FontSize="22" Margin="13,0" VerticalAlignment="Center" Visibility="{Binding IconVisibility}" />
                                                                        </Grid>
                                                                    </Border>
                                                                </DataTemplate>
                                                            </ItemsControl.ItemTemplate>
                                                        </ItemsControl>
                                                    </StackPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <TextBlock Margin="2,4,0,0" FontSize="12" TextWrapping="Wrap" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{x:Static resources:Strings.Menu_Activity_FPSLineNeedsSomethingWatchesGame}" />
                </StackPanel>
            </TabItem>

            <!-- ============================================================ playtime -->
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <ui:SymbolIcon Symbol="DataBarVertical24" Margin="0,0,6,0" />
                        <TextBlock Text="{x:Static resources:Strings.Menu_Activity_Playtime}" />
                    </StackPanel>
                </TabItem.Header>
                <StackPanel Margin="0,12,0,0">
                    <TextBlock FontSize="14" FontWeight="SemiBold" TextWrapping="Wrap" Text="{Binding TotalsText, Mode=OneWay}" />
                    <TextBlock Margin="0,4,0,0" FontSize="12" TextWrapping="Wrap" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{Binding TrackedSinceText, Mode=OneWay}" />

                    <TextBlock Margin="0,18,0,6" FontSize="14" FontWeight="Medium" Text="{x:Static resources:Strings.Menu_Activity_Last14Days}" />
                    <Border Padding="12,12,12,8" CornerRadius="8" BorderThickness="1" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" Background="{DynamicResource ControlFillColorDefaultBrush}">
                        <ItemsControl ItemsSource="{Binding Days, Mode=OneWay}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Rows="1" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Height="162" ToolTip="{Binding ToolTip}" Background="Transparent">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <StackPanel Grid.Row="0" VerticalAlignment="Bottom">
                                            <TextBlock FontSize="10" HorizontalAlignment="Center" Margin="0,0,0,3" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{Binding ValueText}" />
                                            <Border Height="{Binding Height}" Margin="5,0" CornerRadius="3,3,0,0" Opacity="{Binding Opacity}" Background="{DynamicResource SystemAccentColorSecondaryBrush}" />
                                        </StackPanel>
                                        <TextBlock Grid.Row="1" FontSize="11" Margin="0,5,0,0" HorizontalAlignment="Center" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{Binding Label}" />
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Border>

                    <TextBlock Margin="0,18,0,6" FontSize="14" FontWeight="Medium" Text="{x:Static resources:Strings.Menu_Activity_Last12Weeks}" />
                    <Border Padding="12,12,12,8" CornerRadius="8" BorderThickness="1" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" Background="{DynamicResource ControlFillColorDefaultBrush}">
                        <ItemsControl ItemsSource="{Binding Weeks, Mode=OneWay}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Rows="1" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Height="162" ToolTip="{Binding ToolTip}" Background="Transparent">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <StackPanel Grid.Row="0" VerticalAlignment="Bottom">
                                            <TextBlock FontSize="10" HorizontalAlignment="Center" Margin="0,0,0,3" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{Binding ValueText}" />
                                            <Border Height="{Binding Height}" Margin="6,0" CornerRadius="3,3,0,0" Opacity="{Binding Opacity}" Background="{DynamicResource SystemAccentColorSecondaryBrush}" />
                                        </StackPanel>
                                        <TextBlock Grid.Row="1" FontSize="11" Margin="0,5,0,0" HorizontalAlignment="Center" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{Binding Label}" />
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Border>

                    <Grid Margin="0,18,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" FontSize="14" FontWeight="Medium" VerticalAlignment="Center" Text="{x:Static resources:Strings.Menu_Activity_ByGame}" />
                        <ComboBox Grid.Column="1" Width="150" ItemsSource="{Binding RangeOptions, Mode=OneTime}" SelectedItem="{Binding SelectedRange, Mode=TwoWay}" />
                    </Grid>

                    <TextBlock Margin="2,6,0,6" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Visibility="{Binding GamesEmptyVisibility, Mode=OneWay}" Text="{x:Static resources:Strings.Menu_Activity_NothingPlayedPeriod}" />

                    <ItemsControl ItemsSource="{Binding Games, Mode=OneWay}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="220" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" Width="32" Height="32" CornerRadius="5" Background="{DynamicResource ControlFillColorSecondaryBrush}">
                                        <Border.Clip>
                                            <RectangleGeometry Rect="0,0,32,32" RadiusX="5" RadiusY="5" />
                                        </Border.Clip>
                                        <Image Source="{Binding IconUrl}" Stretch="UniformToFill" RenderOptions.BitmapScalingMode="HighQuality" />
                                    </Border>
                                    <StackPanel Grid.Column="1" Margin="10,0,10,0" VerticalAlignment="Center">
                                        <TextBlock FontSize="13" Text="{Binding Name}" TextTrimming="CharacterEllipsis" ToolTip="{Binding Name}" />
                                        <TextBlock FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{Binding Detail}" TextTrimming="CharacterEllipsis" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                        <Border Width="{Binding Width}" Height="14" CornerRadius="3" Background="{DynamicResource SystemAccentColorSecondaryBrush}" />
                                        <TextBlock Margin="8,0,0,0" FontSize="12" VerticalAlignment="Center" Text="{Binding TimeText}" />
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </TabItem>

            <!-- ============================================================ played with -->
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <ui:SymbolIcon Symbol="PeopleCommunity24" Margin="0,0,6,0" />
                        <TextBlock Text="{x:Static resources:Strings.Menu_Activity_Played}" />
                    </StackPanel>
                </TabItem.Header>
                <StackPanel Margin="0,12,0,0">
                    <controls:OptionControl Header="{x:Static resources:Strings.Menu_Activity_NoteWhichFriendsShareMyServer}" Description="{x:Static resources:Strings.Menu_Activity_WhileYouAreGamePhasmaStrapAsks}">
                        <ui:ToggleSwitch IsChecked="{Binding TrackFriends, Mode=TwoWay}" />
                    </controls:OptionControl>

                    <TextBlock Margin="2,14,0,6" TextWrapping="Wrap" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Visibility="{Binding CompanionsEmptyVisibility, Mode=OneWay}" Text="{Binding CompanionsEmptyText, Mode=OneWay}" />

                    <ItemsControl Margin="0,10,0,0" ItemsSource="{Binding Companions, Mode=OneWay}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="0,0,0,6" Padding="10" CornerRadius="8" BorderThickness="1" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" Background="{DynamicResource ControlFillColorDefaultBrush}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <Grid Grid.Column="0" Width="38" Height="38">
                                            <Ellipse Fill="{DynamicResource ControlFillColorSecondaryBrush}" />
                                            <Image Source="{Binding AvatarUrl}" Stretch="UniformToFill" RenderOptions.BitmapScalingMode="HighQuality">
                                                <Image.Clip>
                                                    <EllipseGeometry Center="19,19" RadiusX="19" RadiusY="19" />
                                                </Image.Clip>
                                            </Image>
                                        </Grid>
                                        <StackPanel Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center">
                                            <TextBlock FontSize="13" FontWeight="SemiBold" Text="{Binding Name}" />
                                            <TextBlock FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{Binding Detail}" TextTrimming="CharacterEllipsis" />
                                        </StackPanel>
                                        <TextBlock Grid.Column="2" FontSize="12" VerticalAlignment="Center" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{Binding Together}" />
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </TabItem>
        </TabControl>

        <TextBlock Text="{x:Static resources:Strings.Menu_Notifications_History_Title}" FontSize="20" FontWeight="Medium" Margin="0,22,0,0" />
        <controls:OptionControl Header="{x:Static resources:Strings.Menu_Activity_KeepSessionHistory}" Description="{x:Static resources:Strings.Menu_Activity_WritesDownEachServerYouJoin}">
            <ui:ToggleSwitch IsChecked="{Binding HistoryEnabled, Mode=TwoWay}" />
        </controls:OptionControl>
        <controls:OptionControl Header="{x:Static resources:Strings.Menu_Activity_DeleteHistory}" Description="{x:Static resources:Strings.Menu_Activity_RemovesEveryRecordedSessionScreenshotsClips}">
            <ui:Button Content="{x:Static resources:Strings.Common_Delete}" Icon="Delete24" Command="{Binding ClearHistoryCommand, Mode=OneTime}" />
        </controls:OptionControl>
    </StackPanel>
</ui:UiPage>
