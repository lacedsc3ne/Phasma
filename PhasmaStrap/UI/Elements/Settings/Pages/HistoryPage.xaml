<ui:UiPage x:Class="PhasmaStrap.UI.Elements.Settings.Pages.HistoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:resources="clr-namespace:PhasmaStrap.Resources"
      xmlns:dmodels="clr-namespace:PhasmaStrap.UI.ViewModels.Settings"
      mc:Ignorable="d"
      d:DataContext="{d:DesignInstance dmodels:HistoryViewModel, IsDesignTimeCreatable=True}"
      Title="HistoryPage"
      Scrollable="True">
    <StackPanel Margin="0,0,14,14">
        <TextBlock TextWrapping="Wrap" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{x:Static resources:Strings.Menu_History_EveryGameYouVePlayedThrough}" />

        <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
            <TextBlock VerticalAlignment="Center" Text="{Binding StatusText, Mode=OneWay}" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            <ui:Button Margin="12,0,0,0" Content="{x:Static resources:Strings.Dialog_ChannelLists_Refresh}" Icon="ArrowClockwise24" Command="{Binding RefreshCommand, Mode=OneTime}" />
        </StackPanel>

        <ItemsControl Margin="0,12,0,0" ItemsSource="{Binding Entries, Mode=OneWay}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <ui:Card Margin="0,0,0,6" Padding="0">
                        <Grid VerticalAlignment="Center">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" Width="64" Height="64" CornerRadius="4">
                                <Border.Background>
                                    <ImageBrush ImageSource="{Binding IconUrl, IsAsync=True}" />
                                </Border.Background>
                            </Border>

                            <StackPanel Grid.Column="1" Margin="16,8,12,8" VerticalAlignment="Center">
                                <TextBlock Text="{Binding DisplayName}" FontSize="16" FontWeight="Medium" TextTrimming="CharacterEllipsis" />
                                <TextBlock Foreground="{DynamicResource TextFillColorSecondaryBrush}">
                                    <Run Text="{Binding TotalTimeText, Mode=OneWay}" />
                                    <Run Text="{x:Static resources:Strings.Menu_History_PlayedLastPlayed}" />
                                    <Run Text="{Binding LastPlayedText, Mode=OneWay}" />
                                </TextBlock>
                            </StackPanel>

                            <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="0,0,12,0">
                                <ui:Button Margin="0,0,8,0" Content="{x:Static resources:Strings.Menu_History_CopyLink}" Command="{Binding DataContext.CopyLinkCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}" />
                                <ui:Button Content="{x:Static resources:Strings.Common_Launch}" Icon="Play28" IconFilled="True" Appearance="Success" Foreground="White" IconForeground="White"
                                           Command="{Binding DataContext.LaunchCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}" />
                            </StackPanel>
                        </Grid>
                    </ui:Card>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</ui:UiPage>
