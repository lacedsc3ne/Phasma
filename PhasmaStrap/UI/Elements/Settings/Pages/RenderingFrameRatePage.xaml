<ui:UiPage x:Class="PhasmaStrap.UI.Elements.Settings.Pages.RenderingFrameRatePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:controls="clr-namespace:PhasmaStrap.UI.Elements.Controls"
      xmlns:resources="clr-namespace:PhasmaStrap.Resources"
      mc:Ignorable="d"
      Title="RenderingFrameRatePage"
      Scrollable="True">

    <StackPanel Margin="0,0,14,14">
        <TextBlock FontSize="20" FontWeight="Medium" Text="Frame rate" />

        <TextBlock Text="{x:Static resources:Strings.Menu_Rendering_AntiAliasing}" FontSize="16" FontWeight="SemiBold" Margin="0,12,0,0" />
        <TextBlock Text="{x:Static resources:Strings.Menu_Rendering_AppliesGPUShaderOverRobloxWindow}" TextWrapping="Wrap" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />

        <controls:OptionControl Header="{x:Static resources:Strings.Menu_Rendering_EnableAntiAliasingOverlay}" Description="{x:Static resources:Strings.Menu_Rendering_OffByDefaultSinceExtraRendering}">
            <ui:ToggleSwitch IsChecked="{Binding AntiAliasingEnabled, Mode=TwoWay}" />
        </controls:OptionControl>

        <controls:OptionControl Header="{x:Static resources:Strings.Menu_Rendering_AntiAliasingMethod}" Description="{x:Static resources:Strings.Menu_Rendering_WhichShaderTechniqueApplyOnceOverlay}">
            <ComboBox Padding="10,5,10,5" MinWidth="160" IsEnabled="{Binding AntiAliasingEnabled}" ItemsSource="{Binding AntiAliasingMethodNames, Mode=OneTime}" SelectedIndex="{Binding AntiAliasingMethodIndex, Mode=TwoWay}" />
        </controls:OptionControl>

        <TextBlock Text="{x:Static resources:Strings.Menu_Rendering_FrameGeneration}" FontSize="16" FontWeight="SemiBold" Margin="0,16,0,0" />
        <TextBlock Text="{x:Static resources:Strings.Menu_Rendering_ShaderBasedMotionInterpolationDrawnClick}" TextWrapping="Wrap" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />

        <controls:OptionControl Header="{x:Static resources:Strings.Menu_Rendering_EnableFrameGeneration}" Description="{x:Static resources:Strings.Menu_Rendering_InterpolatesExtraFrameBetweenRealCaptured}">
            <ui:ToggleSwitch IsChecked="{Binding FrameGenEnabled, Mode=TwoWay}" />
        </controls:OptionControl>

        <controls:OptionControl Header="{x:Static resources:Strings.Menu_Rendering_FrameGenerationQuality}" Description="{x:Static resources:Strings.Menu_Rendering_HigherQualityCostsMoreGPUTime}">
            <StackPanel Orientation="Horizontal">
                <Slider Width="200" Minimum="0" Maximum="2" TickFrequency="1" IsSnapToTickEnabled="True" Value="{Binding FrameGenQuality, Mode=TwoWay}" />
                <TextBlock VerticalAlignment="Center" Margin="8,0,0,0" Text="{Binding FrameGenQualityDisplay, Mode=OneWay}" />
            </StackPanel>
        </controls:OptionControl>

    </StackPanel>
</ui:UiPage>
