<base:WpfUiWindow x:Class="PhasmaStrap.UI.Elements.Dialogs.AddNvidiaCustomSettingDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:base="clr-namespace:PhasmaStrap.UI.Elements.Base"
        xmlns:resources="clr-namespace:PhasmaStrap.Resources"
        mc:Ignorable="d"
        Title="{x:Static resources:Strings.Dialog_AddNvidiaCustomSetting_Title}"
        Width="420"
        MinHeight="0"
        SizeToContent="Height"
        ResizeMode="NoResize"
        Background="{ui:ThemeResource ApplicationBackgroundBrush}"
        ExtendsContentIntoTitleBar="True"
        WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Padding="8" Title="{x:Static resources:Strings.Dialog_AddNvidiaCustomSetting_Title}" ShowMinimize="False" ShowMaximize="False" CanMaximize="False" KeyboardNavigation.TabNavigation="None" />

        <StackPanel Grid.Row="1" Margin="20,12">
            <TextBlock TextWrapping="Wrap" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{x:Static resources:Strings.Dialog_AddNvidiaCustomSetting_Description}" Margin="0,0,0,12" />

            <TextBlock Text="{x:Static resources:Strings.Common_Name}" Margin="0,0,0,4" />
            <TextBox x:Name="NameTextBox" Margin="0,0,0,12" />

            <TextBlock Text="{x:Static resources:Strings.Dialog_AddNvidiaCustomSetting_SettingId}" Margin="0,0,0,4" />
            <TextBox x:Name="SettingIdTextBox" Margin="0,0,0,12" />

            <TextBlock Text="{x:Static resources:Strings.Common_Value}" Margin="0,0,0,4" />
            <TextBox x:Name="ValueTextBox" Margin="0,0,0,4" />
            <TextBlock TextWrapping="Wrap" FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Text="{x:Static resources:Strings.Dialog_AddNvidiaCustomSetting_ValueHint}" />

            <TextBlock x:Name="StatusText" Margin="0,12,0,0" TextWrapping="Wrap" Foreground="{DynamicResource SystemFillColorCriticalBrush}" />
        </StackPanel>

        <Border Grid.Row="2" Margin="0,10,0,0" Padding="15" Background="{ui:ThemeResource SolidBackgroundFillColorSecondaryBrush}">
            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                <Button MinWidth="100" Content="{x:Static resources:Strings.Common_OK}" Click="OKButton_Click" />
                <Button MinWidth="100" Margin="12,0,0,0" Content="{x:Static resources:Strings.Common_Cancel}" IsCancel="True" />
            </StackPanel>
        </Border>
    </Grid>
</base:WpfUiWindow>
