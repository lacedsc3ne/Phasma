<base:WpfUiWindow x:Class="PhasmaStrap.UI.Elements.Dialogs.GoogleFontsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:PhasmaStrap.UI.Elements.Dialogs"
        xmlns:base="clr-namespace:PhasmaStrap.UI.Elements.Base"
        xmlns:resources="clr-namespace:PhasmaStrap.Resources"
        mc:Ignorable="d"
        Title="{x:Static resources:Strings.Dialog_GoogleFonts_Title}"
        MinWidth="0"
        MinHeight="0"
        Width="460"
        Height="560"
        Background="{ui:ThemeResource ApplicationBackgroundBrush}"
        ExtendsContentIntoTitleBar="True"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Padding="8" Title="{x:Static resources:Strings.Dialog_GoogleFonts_Title}" ShowMinimize="False" ShowMaximize="False" CanMaximize="False" KeyboardNavigation.TabNavigation="None" Icon="pack://application:,,,/PhasmaStrap.ico" />

        <Grid Grid.Row="1" Margin="16,8,16,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="{x:Static resources:Strings.Dialog_GoogleFonts_Header}" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10" />

            <ui:TextBox x:Name="SearchTextBox" Grid.Row="1" Icon="Search32" PlaceholderText="{x:Static resources:Strings.Dialog_GoogleFonts_SearchWatermark}" TextChanged="SearchTextBox_TextChanged" Margin="0,0,0,8" />

            <Grid Grid.Row="2">
                <ListBox x:Name="FontListBox" BorderThickness="0" Background="Transparent" SelectionChanged="FontListBox_SelectionChanged" ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding DisplayName}" Padding="4,6" />
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <ui:ProgressRing x:Name="LoadingRing" Width="32" Height="32" IsIndeterminate="True" Visibility="Collapsed" />

                <TextBlock x:Name="NoResultsText" Text="{x:Static resources:Strings.Dialog_GoogleFonts_NoResults}" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,20,0,0" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Visibility="Collapsed" />
            </Grid>

            <Border Grid.Row="3" Margin="0,10,0,0" Padding="10" MinHeight="52" Background="{ui:ThemeResource SolidBackgroundFillColorSecondaryBrush}">
                <Grid>
                    <TextBlock x:Name="PreviewText" Text="{x:Static resources:Strings.Dialog_GoogleFonts_Preview}" FontSize="18" TextWrapping="Wrap" VerticalAlignment="Center" />
                    <ui:ProgressRing x:Name="PreviewLoadingRing" Width="18" Height="18" IsIndeterminate="True" HorizontalAlignment="Right" VerticalAlignment="Center" Visibility="Collapsed" />
                </Grid>
            </Border>

            <CheckBox x:Name="ApplyToAppCheckBox" Grid.Row="4" Content="{x:Static resources:Strings.Dialog_GoogleFonts_ApplyToApp}" Margin="0,10,0,0" />
        </Grid>

        <Border Grid.Row="2" Margin="0,10,0,0" Padding="15" Background="{ui:ThemeResource SolidBackgroundFillColorSecondaryBrush}">
            <StackPanel Orientation="Horizontal" FlowDirection="LeftToRight" HorizontalAlignment="Right">
                <Button x:Name="ApplyButton" MinWidth="100" Content="{x:Static resources:Strings.Dialog_GoogleFonts_Apply}" Click="ApplyButton_Click" IsEnabled="False" />
                <Button MinWidth="100" Margin="12,0,0,0" Content="{x:Static resources:Strings.Common_Cancel}" IsCancel="True" />
            </StackPanel>
        </Border>
    </Grid>
</base:WpfUiWindow>
