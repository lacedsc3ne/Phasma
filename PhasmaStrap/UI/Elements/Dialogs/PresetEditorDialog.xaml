<base:WpfUiWindow x:Class="PhasmaStrap.UI.Elements.Dialogs.PresetEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:base="clr-namespace:PhasmaStrap.UI.Elements.Base"
        mc:Ignorable="d"
        Title="Edit preset"
        Width="760"
        Height="560"
        MinWidth="560"
        MinHeight="400"
        Background="{ui:ThemeResource ApplicationBackgroundBrush}"
        ExtendsContentIntoTitleBar="True"
        WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Padding="8" x:Name="RootTitleBar" Title="Edit preset" ShowMinimize="False" ShowMaximize="False" Icon="pack://application:,,,/PhasmaStrap.ico" />

        <StackPanel Grid.Row="1" Margin="16,8,16,0">
            <TextBlock x:Name="HeaderText" FontSize="20" FontWeight="Medium" Text="Preset" />
            <TextBlock TextWrapping="Wrap" Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                       Text="Double-click a cell to change a flag's name or value. These flags apply on top of your global flags for every place this preset is assigned to. Nothing is written until you press Save." />
        </StackPanel>

        <WrapPanel Grid.Row="2" Margin="16,12,16,8">
            <ui:TextBox x:Name="NewNameBox" Width="260" Margin="0,0,8,6" PlaceholderText="Flag name (e.g. DFIntTaskSchedulerTargetFps)" />
            <ui:TextBox x:Name="NewValueBox" Width="140" Margin="0,0,8,6" PlaceholderText="Value" />
            <ui:Button Margin="0,0,8,6" Content="Add flag" Icon="Add24" Click="AddButton_Click" />
            <ui:Button x:Name="DeleteButton" Margin="0,0,8,6" Content="Delete selected" Icon="Delete24" Appearance="Danger" Click="DeleteButton_Click" IsEnabled="False" />
        </WrapPanel>

        <DataGrid x:Name="FlagsGrid" Grid.Row="3" Margin="16,0,16,0" HeadersVisibility="Column" GridLinesVisibility="Horizontal" AutoGenerateColumns="False"
                  CanUserAddRows="False" CanUserDeleteRows="False" CanUserReorderColumns="False" SelectionChanged="FlagsGrid_SelectionChanged">
            <DataGrid.Style>
                <Style TargetType="DataGrid" BasedOn="{StaticResource {x:Type DataGrid}}">
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="BorderThickness" Value="0" />
                </Style>
            </DataGrid.Style>
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="{DynamicResource ControlFillColorTertiaryBrush}" />
                    <Setter Property="Height" Value="32" />
                    <Setter Property="Padding" Value="8,0,8,0" />
                    <Setter Property="BorderBrush" Value="{DynamicResource ControlElevationBorderBrush}" />
                    <Setter Property="BorderThickness" Value="1" />
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Width="2*" />
                <DataGridTextColumn Header="Value" Binding="{Binding Value, UpdateSourceTrigger=PropertyChanged}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="4" Margin="16,12,16,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="StatusText" Grid.Column="0" VerticalAlignment="Center" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            <ui:Button Grid.Column="1" Margin="0,0,8,0" Content="Save" Icon="Save24" Appearance="Primary" Click="SaveButton_Click" />
            <ui:Button Grid.Column="2" Content="Cancel" Click="CancelButton_Click" />
        </Grid>
    </Grid>
</base:WpfUiWindow>
