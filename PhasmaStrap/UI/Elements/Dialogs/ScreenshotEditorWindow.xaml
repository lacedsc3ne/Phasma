<base:WpfUiWindow x:Class="PhasmaStrap.UI.Elements.Dialogs.ScreenshotEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:base="clr-namespace:PhasmaStrap.UI.Elements.Base"
        mc:Ignorable="d"
        Title="Edit screenshot"
        Width="1180"
        Height="780"
        MinWidth="820"
        MinHeight="560"
        Background="{ui:ThemeResource ApplicationBackgroundBrush}"
        ExtendsContentIntoTitleBar="True"
        WindowStartupLocation="CenterOwner"
        PreviewKeyDown="Window_PreviewKeyDown">
    <base:WpfUiWindow.Resources>
        <Style x:Key="ToolButton" TargetType="ui:Button" BasedOn="{StaticResource {x:Type ui:Button}}">
            <Setter Property="Margin" Value="0,0,4,0" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="Appearance" Value="Secondary" />
            <Setter Property="Focusable" Value="False" />
        </Style>
        <Style x:Key="Swatch" TargetType="Border">
            <Setter Property="Width" Value="22" />
            <Setter Property="Height" Value="22" />
            <Setter Property="CornerRadius" Value="11" />
            <Setter Property="Margin" Value="0,0,5,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="BorderThickness" Value="2" />
            <Setter Property="BorderBrush" Value="Transparent" />
        </Style>
    </base:WpfUiWindow.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Padding="8" x:Name="RootTitleBar" Title="Edit screenshot" Icon="pack://application:,,,/PhasmaStrap.ico" />

        <!-- toolbar -->
        <Border Grid.Row="1" Margin="12,4,12,8" Padding="10,8" CornerRadius="8" Background="{DynamicResource ControlFillColorDefaultBrush}" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" BorderThickness="1">
            <WrapPanel VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" Margin="0,0,14,0">
                    <ui:Button x:Name="ToolCrop" Style="{StaticResource ToolButton}" Icon="Crop24" Content="Crop" Tag="Crop" Click="Tool_Click" ToolTip="Drag a rectangle, then press Apply crop (C)" />
                    <ui:Button x:Name="ToolPen" Style="{StaticResource ToolButton}" Icon="Pen24" Content="Pen" Tag="Pen" Click="Tool_Click" ToolTip="Freehand drawing (P)" />
                    <ui:Button x:Name="ToolArrow" Style="{StaticResource ToolButton}" Icon="ArrowRight24" Content="Arrow" Tag="Arrow" Click="Tool_Click" ToolTip="Drag from tail to head (A)" />
                    <ui:Button x:Name="ToolRect" Style="{StaticResource ToolButton}" Icon="RectangleLandscape24" Content="Box" Tag="Rect" Click="Tool_Click" ToolTip="Rectangle outline (R)" />
                    <ui:Button x:Name="ToolEllipse" Style="{StaticResource ToolButton}" Icon="Circle24" Content="Ellipse" Tag="Ellipse" Click="Tool_Click" ToolTip="Ellipse outline (E)" />
                    <ui:Button x:Name="ToolText" Style="{StaticResource ToolButton}" Icon="TextT24" Content="Text" Tag="Text" Click="Tool_Click" ToolTip="Click where the text should start, type, press Enter (T)" />
                    <ui:Button x:Name="ToolPixelate" Style="{StaticResource ToolButton}" Icon="Blur24" Content="Pixelate" Tag="Pixelate" Click="Tool_Click" ToolTip="Drag over something to hide it - names, chat, etc. (X)" />
                </StackPanel>

                <StackPanel x:Name="SwatchPanel" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,14,0">
                    <Border Style="{StaticResource Swatch}" Background="#FF3B30" Tag="#FF3B30" MouseLeftButtonDown="Swatch_Click" />
                    <Border Style="{StaticResource Swatch}" Background="#FF9500" Tag="#FF9500" MouseLeftButtonDown="Swatch_Click" />
                    <Border Style="{StaticResource Swatch}" Background="#FFD60A" Tag="#FFD60A" MouseLeftButtonDown="Swatch_Click" />
                    <Border Style="{StaticResource Swatch}" Background="#30D158" Tag="#30D158" MouseLeftButtonDown="Swatch_Click" />
                    <Border Style="{StaticResource Swatch}" Background="#0A84FF" Tag="#0A84FF" MouseLeftButtonDown="Swatch_Click" />
                    <Border Style="{StaticResource Swatch}" Background="#BF5AF2" Tag="#BF5AF2" MouseLeftButtonDown="Swatch_Click" />
                    <Border Style="{StaticResource Swatch}" Background="#FFFFFF" Tag="#FFFFFF" MouseLeftButtonDown="Swatch_Click" />
                    <Border Style="{StaticResource Swatch}" Background="#000000" Tag="#000000" MouseLeftButtonDown="Swatch_Click" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,14,0">
                    <TextBlock Text="Size" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    <Slider x:Name="SizeSlider" Width="110" Minimum="1" Maximum="24" Value="4" VerticalAlignment="Center" />
                    <TextBlock Text="{Binding ElementName=SizeSlider, Path=Value, StringFormat={}{0:0}}" VerticalAlignment="Center" Margin="6,0,0,0" Width="22" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,14,0">
                    <ui:Button x:Name="UndoButton" Style="{StaticResource ToolButton}" Icon="ArrowUndo24" ToolTip="Undo (Ctrl+Z)" Click="Undo_Click" />
                    <ui:Button x:Name="RedoButton" Style="{StaticResource ToolButton}" Icon="ArrowRedo24" ToolTip="Redo (Ctrl+Y)" Click="Redo_Click" />
                    <ui:Button x:Name="ApplyCropButton" Style="{StaticResource ToolButton}" Icon="Crop24" Content="Apply crop" Appearance="Primary" Click="ApplyCrop_Click" Visibility="Collapsed" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Zoom" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    <Slider x:Name="ZoomSlider" Width="110" Minimum="0.1" Maximum="4" Value="1" VerticalAlignment="Center" ValueChanged="ZoomSlider_ValueChanged" />
                    <ui:Button Style="{StaticResource ToolButton}" Content="Fit" Margin="8,0,0,0" Click="ZoomFit_Click" />
                </StackPanel>
            </WrapPanel>
        </Border>

        <!-- canvas -->
        <Border Grid.Row="2" Margin="12,0,12,0" CornerRadius="8" Background="#0A000000" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" BorderThickness="1" ClipToBounds="True">
            <ScrollViewer x:Name="Scroller" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" PreviewMouseWheel="Scroller_PreviewMouseWheel">
                <Grid x:Name="Surface" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="16">
                    <Image x:Name="BaseImage" Stretch="None" RenderOptions.BitmapScalingMode="HighQuality" />
                    <Canvas x:Name="Overlay" Background="Transparent" ClipToBounds="True"
                            MouseLeftButtonDown="Overlay_MouseLeftButtonDown" MouseMove="Overlay_MouseMove" MouseLeftButtonUp="Overlay_MouseLeftButtonUp" />
                    <Canvas x:Name="Preview" IsHitTestVisible="False" ClipToBounds="True" />
                    <Canvas x:Name="TextLayer" ClipToBounds="True" IsHitTestVisible="False">
                        <TextBox x:Name="TextInput" Visibility="Collapsed" MinWidth="120" Background="#66000000" BorderThickness="1" IsHitTestVisible="True"
                                 KeyDown="TextInput_KeyDown" LostKeyboardFocus="TextInput_LostFocus" />
                    </Canvas>
                </Grid>
            </ScrollViewer>
        </Border>

        <!-- footer -->
        <Grid Grid.Row="3" Margin="12,10,12,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="StatusText" Grid.Column="0" VerticalAlignment="Center" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextTrimming="CharacterEllipsis" />
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <ui:Button Margin="0,0,8,0" Content="Make GIF" Icon="Gif24" ToolTip="Turn this screenshot (with your edits) into a GIF - zoom, pan, or a slideshow with other screenshots" Click="MakeGif_Click" />
                <ui:Button Margin="0,0,8,0" Content="Copy" Icon="Copy24" ToolTip="Copy the edited image to the clipboard (Ctrl+C)" Click="Copy_Click" />
                <ui:Button Margin="0,0,8,0" Content="Save as copy" Icon="Save24" ToolTip="Keep the original and write a new file next to it" Click="SaveCopy_Click" />
                <ui:Button Margin="0,0,8,0" Content="Save" Icon="Save24" Appearance="Primary" ToolTip="Overwrite the original screenshot (Ctrl+S)" Click="Save_Click" />
                <ui:Button Content="Close" Click="Close_Click" />
            </StackPanel>
        </Grid>
    </Grid>
</base:WpfUiWindow>
